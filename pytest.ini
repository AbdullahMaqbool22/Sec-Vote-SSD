[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --cov=services
    --cov=shared
    --cov=gateway
    --cov-report=term-missing
    --cov-report=html
